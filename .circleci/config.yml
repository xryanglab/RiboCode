version: 2
jobs:
  build:
    docker:
      - image: ubuntu:14.04 
    steps:
      - run: |
          sudo apt-get update
          sudo apt-get install -y hdf5-tools libhdf5-serial-dev
      - checkout
      - run:
          name: test
          command: ./run_test_travis.sh
