language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
sudo: required
before_install:
  - sudo apt-get install -y libhdf5-serial-dev
  - sudo apt-get install -y hdf5-tools
install:
  - pip install .
script:
  - ./run_test_travis.sh
